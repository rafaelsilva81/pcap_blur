from scapy.all import Ether, IP, TCP, UDP, Raw, wrpcap, Packet
from typing import Dict
import random

"""
  Esse arquivo cria um pacote de teste com dados aleatórios,
  que pode ser usado para testar a anonimização e testar
  casos de exceção. 
"""

